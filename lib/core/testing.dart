/*
inputs to test
  magnitude
    empty(0 number)
    single(1 number)
    multiple(2 number)
    enough(max ,or one less than count)
    limit(exactly count)
    excess(one more than count)
  in-variants and pre-conditions/assertions */
